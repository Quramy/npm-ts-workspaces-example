{
  "name": "lerna-npm-workspaces-example",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "rimraf \"packages/**/lib\" \"packages/**/*.tsbuildinfo\" tsconfig.build.json",
    "test": "lerna run test",
    "compile": "tsc -b tsconfig.build.json",
    "prepare": "npm run compile"
  },
  "devDependencies": {
    "@types/node": "^14.11.8",
    "rimraf": "^3.0.2",
    "typescript": "~4.0.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
