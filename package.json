{
  "name": "lerna-npm-workspaces-example",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "prepare": "tsc -b tsconfig.build.json",
    "clean": "rimraf \"packages/**/lib\" \"packages/**/*.tsbuildinfo\""
  },
  "devDependencies": {
    "@types/node": "^14.11.8",
    "rimraf": "^3.0.2",
    "typescript": "~4.0.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
